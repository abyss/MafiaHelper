exports.run = function (bot, msg, args) {
    // Unlock the channel
    // Start the timer (24h?)
    // Set the majority based on role.
};

exports.info = {
    name: 'startday',
    usage: 'startday',
    description: 'Starts the mafia day by unlocking the channel, starting the timer, and setting the majority.'
};
